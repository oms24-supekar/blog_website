<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create Post</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <main class="container form-page">
      <h1>Create a New Post</h1>
      <% if (typeof error !== 'undefined' && error) { %>
      <p class="error"><%= error %></p>
      <% } %>
      <form class="post-form" action="/posts" method="POST">
        <label for="title">Title</label>
        <input id="title" name="title" required value="<%= (values && values.title) || '' %>" />

        <label for="author">Author</label>
        <input id="author" name="author" value="<%= (values && values.author) || '' %>" />

        <label for="content">Content</label>
        <textarea id="content" name="content" rows="10" required><%= (values && values.content) || '' %></textarea>

        <div class="actions">
          <button class="button button-primary" type="submit">Publish Post</button>
          <a class="button" href="/">Cancel</a>
        </div>
      </form>
    </main>
  </body>
</html>
